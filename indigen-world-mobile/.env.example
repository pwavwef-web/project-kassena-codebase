# Indigen World Mobile — environment template.
# Copy to .env and load with a package such as flutter_dotenv (added later).
# NEVER commit real values. Only client-safe config belongs here.

FIREBASE_API_KEY=your_api_key_here
FIREBASE_APP_ID=your_app_id
FIREBASE_PROJECT_ID=your_project_id
FIREBASE_MESSAGING_SENDER_ID=your_sender_id
